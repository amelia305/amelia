rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Phase 0 stub — all access denied. Real rules in Phase 1.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
